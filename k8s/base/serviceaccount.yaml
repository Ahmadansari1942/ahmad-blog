apiVersion: v1
kind: ServiceAccount
metadata:
  name: ahmad-blog
  labels:
    app.kubernetes.io/name: ahmad-blog
# The app never talks to the Kubernetes API, so don't mount a token.
automountServiceAccountToken: false
